import React from 'react';
import { Spinner, Wrapper } from './Loader.styles';

const Loader = () => {
    return (
        <Wrapper>
            <Spinner />
        </Wrapper>
    );
};

export default Loader;
